<div class="container artists">
  <div class="row">
    <div class="col-sm-12">
      <header>
        <h1>Artistas</h1>
      </header>

      <div class="artists-list">
      <% @artists.each do |artist| %>
        <div class="artist">
          <div class="row">
            <div class="card" style="width: 18rem;">
              <img src="<%= artist.image_url %>" class="card-img-top" alt="<%= artist.name %>">
              <div class="card-body">
                <h5 class="card-title"><%= artist.name %></h5>
                <a href="<%= link_to "<span class='glyphicon glyphicon-pencil'><span>".html_safe, artists_path(artist), class: "btn btn-primary"%> Ver más </a>
              </div>
            </div>
          </div>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
